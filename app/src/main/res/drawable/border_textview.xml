<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="1dp"
        android:color="@color/black" /> <!-- Màu và độ dày của đường viền -->
    <corners android:radius="30dp" /> <!-- Độ cong của góc -->
</shape>
